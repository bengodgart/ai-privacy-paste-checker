# Parking lot

Mid-build ideas that came up while scoping v1, not built, not scheduled. From the brief's own "expansion paths":

- A tiny embeddable badge or snippet a small business could paste next to its internal "AI use policy" so staff get the check in context. Future option, not v1 scope.
- A short methods write-up ("what counts as sensitive, and why client-side detection is the honest way to check it") that becomes a second portfolio piece. Future option, not v1 scope.
- Detecting secrets embedded inside a URL's query string or file name.
- A wider keyword list (the current 12 health and financial terms are curated on purpose, not exhaustive).
